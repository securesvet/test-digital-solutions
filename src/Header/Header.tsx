import Input from "../SearchInput/SearchInput";
import "./Header.css";

const Header = () => {
    return (
        <div className="header">
            <Input />
        </div>
    );
};

export default Header;
